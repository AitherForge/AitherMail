const BACKEND_URL='https://aither-backend.onrender.com';
const MAIL_API=(window.AITHER_MAIL_API||'https://aither-mail-backend.onrender.com').replace(/\/$/,'');
let aitherUser=null,messages=[],selected=null,search='',box='inbox';
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function backend(path,opts={}){const r=await fetch(BACKEND_URL+path,{credentials:'include',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{}if(!r.ok)throw Error(d.detail||`AitherBackend error ${r.status}`);return d}
async function session(){try{const d=await backend('/api/auth/session');if(d.authenticated){aitherUser=d.user;return true}}catch(e){}return false}
function authScreen(){document.body.innerHTML=`<main class="signedout"><section class="login-card"><div class="logo">Aither<span>Mail</span></div><p>Your Aither email workspace.</p><div class="auth-tabs"><button id="loginTab" class="active" onclick="authMode('login')">Sign in</button><button id="registerTab" onclick="authMode('register')">Create account</button></div><form id="authForm" onsubmit="submitAuth(event)"></form><p id="authError" class="error hidden"></p></section></main>`;authMode('login')}
function authMode(mode){$('#loginTab').classList.toggle('active',mode==='login');$('#registerTab').classList.toggle('active',mode==='register');$('#authForm').innerHTML=mode==='register'?`<input id="authName" required maxlength="80" placeholder="Name"><input id="authEmail" required type="email" placeholder="Email"><input id="authPassword" required minlength="8" type="password" placeholder="Password"><button class="send full">Create Aither Account</button>`:`<input id="authEmail" required type="email" placeholder="Email"><input id="authPassword" required type="password" placeholder="Password"><button class="send full">Sign in</button>`}
async function submitAuth(e){e.preventDefault();const reg=!!$('#authName'),b=e.submitter;b.disabled=true;try{const body=reg?{name:$('#authName').value.trim(),email:$('#authEmail').value.trim(),password:$('#authPassword').value}:{email:$('#authEmail').value.trim(),password:$('#authPassword').value};const d=await backend(reg?'/api/auth/register':'/api/auth/login',{method:'POST',body:JSON.stringify(body)});aitherUser=d.user;renderApp();await refresh()}catch(x){const er=$('#authError');er.textContent=x.message;er.classList.remove('hidden');b.disabled=false}}
function renderApp(){document.body.innerHTML=`<div class="mailapp"><aside class="rail" id="rail"><div class="brand">Aither<span>Mail</span></div><div class="accountcard"><b>${esc(aitherUser?.name||'Aither User')}</b><small>${esc(aitherUser?.email||'')}</small></div><button class="compose" onclick="compose()">＋ Compose</button><nav><button class="folder active" data-box="inbox" onclick="setBox('inbox')">▣ <span>Inbox</span><b id="inboxCount"></b></button><button class="folder" data-box="sent" onclick="setBox('sent')">➤ <span>Sent</span></button><button class="folder" data-box="all" onclick="setBox('all')">✉ <span>All Mail</span></button></nav><div class="railbottom"><button onclick="showSettings()">⚙ Settings</button><button onclick="logout()">↪ Sign out</button></div></aside><main class="workspace"><header class="header"><button class="mobile" onclick="$('#rail').classList.toggle('open')">☰</button><form class="search" onsubmit="doSearch(event)"><span>⌕</span><input id="search" placeholder="Search mail" autocomplete="off"><button>Search</button></form><button class="iconbtn" onclick="refresh()" title="Refresh">↻</button></header><section class="toolbar"><label><input id="selectAll" type="checkbox" onchange="selectAll(this.checked)"> Select all</label><button onclick="deleteSelected()">Delete</button><button onclick="refresh()">Refresh</button><span id="status"></span></section><section class="content"><div class="message-list" id="messageList"></div><article class="message-view" id="messageView"><div class="empty"><strong>Aither Mail v4</strong><span>Select an email to read it.</span></div></article></section></main></div>`}
function setBox(v){box=v;document.querySelectorAll('.folder').forEach(x=>x.classList.toggle('active',x.dataset.box===v));selected=null;renderList()}
async function mail(path,opts={}){const r=await fetch(MAIL_API+path,{...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});if(!r.ok)throw Error(`Mail service returned ${r.status}`);return r.status===204?null:r.json()}
function normalize(m){const c=m.Content||m.content||{};const h=c.Headers||c.headers||{};const first=n=>{const v=h[n]||h[n.toLowerCase()]||[];return Array.isArray(v)?v[0]||'':v};return{id:m.ID||m.id,from:first('From'),to:first('To'),subject:first('Subject')||'(no subject)',date:c.Date||c.date||m.Created||m.created||'',text:c.Body||c.body||'',html:c.MIME||c.mime||'',raw:m}}
async function refresh(){try{setStatus('Connecting…');const d=await mail('/api/v2/messages?start=0&limit=100');messages=(d.items||d||[]).map(normalize);$('#inboxCount').textContent=messages.length;renderList();setStatus(`${messages.length} messages`)}catch(e){setStatus(e.message);renderListError(e)}}
function filtered(){let a=messages;if(search)a=a.filter(m=>(m.from+' '+m.to+' '+m.subject+' '+m.text).toLowerCase().includes(search.toLowerCase()));if(box==='sent')a=a.filter(m=>m.from.toLowerCase().includes((aitherUser?.email||'').toLowerCase()));return a}
function renderList(){const list=$('#messageList');if(!list)return;const a=filtered();if(!a.length){list.innerHTML='<div class="empty"><strong>No messages</strong><span>Your MailHog-compatible inbox is empty.</span></div>';return}list.innerHTML=a.map(m=>`<button class="mailrow ${selected===m.id?'selected':''}" data-id="${esc(m.id)}" onclick="openMessage('${esc(m.id)}')"><input type="checkbox" onclick="event.stopPropagation()" data-select="${esc(m.id)}"><div class="avatar">${esc((displaySender(m.from)||'?')[0].toUpperCase())}</div><div class="rowcopy"><b>${esc(displaySender(m.from)||'Unknown sender')}</b><strong>${esc(m.subject)}</strong><span>${esc(strip(m.text).slice(0,120))}</span></div><time>${date(m.date)}</time></button>`).join('');list.querySelectorAll('[data-select]').forEach(x=>x.addEventListener('change',updateSelected))}
function renderListError(e){$('#messageList').innerHTML=`<div class="empty"><strong>Mail service unavailable</strong><span>${esc(e.message)}</span><button class="secondary" onclick="refresh()">Try again</button></div>`}
async function openMessage(id){selected=id;renderList();const local=messages.find(x=>x.id===id);if(!local)return;let m=local;try{m=normalize(await mail('/api/v1/messages/'+encodeURIComponent(id)))}catch(e){}const body=m.html?extractBody(m.html):escapeText(m.text);$('#messageView').innerHTML=`<div class="viewhead"><button class="mobile" onclick="closeMessage()">←</button><div><h1>${esc(m.subject)}</h1><p>${esc(displaySender(m.from))} → ${esc(m.to)}</p><small>${esc(m.date)}</small></div><div class="viewactions"><button onclick="deleteMessage('${esc(m.id)}')">Delete</button><button onclick="compose('${esc(m.from)}','Re: ${esc(m.subject)}')">Reply</button></div></div><div class="emailbody"><iframe sandbox class="email-frame" title="Email content"></iframe></div><details class="source"><summary>View raw message</summary><pre>${esc(JSON.stringify(m.raw,null,2))}</pre></details>`;$('#messageView iframe').srcdoc=body}
function closeMessage(){$('#messageView').innerHTML='<div class="empty"><strong>Aither Mail v4</strong><span>Select an email to read it.</span></div>';selected=null;renderList()}
async function deleteMessage(id){try{await mail('/api/v1/messages/'+encodeURIComponent(id),{method:'DELETE'});toast('Message deleted');closeMessage();await refresh()}catch(e){toast(e.message)}}
async function deleteSelected(){const ids=[...document.querySelectorAll('[data-select]:checked')].map(x=>x.dataset.select);if(!ids.length){toast('Select messages first');return}for(const id of ids)await deleteMessage(id);await refresh()}
function updateSelected(){const n=document.querySelectorAll('[data-select]:checked').length;setStatus(n?`${n} selected`:`${messages.length} messages`)}
function selectAll(on){document.querySelectorAll('[data-select]').forEach(x=>x.checked=on);updateSelected()}
function compose(to='',subject=''){const modal=document.createElement('div');modal.className='modal';modal.innerHTML=`<section class="composebox"><header>New message <button onclick="this.closest('.modal').remove()">×</button></header><input id="cto" placeholder="To" value="${esc(to)}"><input id="csubject" placeholder="Subject" value="${esc(subject)}"><textarea id="cbody" placeholder="Write your message…"></textarea><footer><button class="send" onclick="sendMessage(this)">Send</button></footer></section>`;document.body.appendChild(modal)}
async function sendMessage(btn){btn.disabled=true;const to=$('#cto').value.trim(),subject=$('#csubject').value,body=$('#cbody').value;if(!to){toast('Add a recipient');btn.disabled=false;return}try{await mail('/api/v1/send',{method:'POST',body:JSON.stringify({from:aitherUser?.email||'aither@localhost',to,subject,body})});btn.closest('.modal').remove();toast('Message accepted by Aither Mail');refresh()}catch(e){toast('Sending needs the Aither Mail SMTP/API bridge: '+e.message);btn.disabled=false}}
function extractBody(v){const s=String(v);const i=s.indexOf('\r\n\r\n');return i>=0?s.slice(i+4):s.replace(/^[\s\S]*?<body[^>]*>/i,'').replace(/<\/body>[\s\S]*$/i,'')}
function escapeText(v){return esc(v).replace(/\r?\n/g,'<br>')}
function strip(v){return String(v||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim()}
function displaySender(v){const m=String(v||'').match(/^(.*?)\s*<([^>]+)>$/);return m?(m[1].replace(/^"|"$/g,'')||m[2]):v}
function date(v){const d=new Date(v);return isNaN(d)?String(v||'').slice(0,16):d.toLocaleDateString(undefined,{month:'short',day:'numeric'})}
function doSearch(e){e.preventDefault();search=$('#search').value.trim();renderList()}
function setStatus(v){const x=$('#status');if(x)x.textContent=v}
function toast(v){const x=document.createElement('div');x.className='toast';x.textContent=v;document.body.appendChild(x);setTimeout(()=>x.remove(),3000)}
function showSettings(){toast('MailHog-compatible API mode is active. SMTP configuration is handled by the mail backend.')}
async function logout(){try{await backend('/api/auth/logout',{method:'POST'})}catch(e){}location.reload()}
async function boot(){if(await session()){renderApp();await refresh();setInterval(refresh,5000)}else authScreen()}
boot();